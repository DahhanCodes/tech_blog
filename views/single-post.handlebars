{{> post-info post}}
<!--only if logged in will be able to create a post-->
{{#if loggedIn}}
<div class="card">
    <h3 class="card-header"></h3>
    <form class="card-body comment-form" id="comment-form">

        <label class="form-label">Comment</label>
        <input id="comment" name="comment-body" class="form-input" />

        <button type="submit" class="btn">Submit</button>
    </form>
</div>
{{/if}}

{{> comments post.comments}}

<!--only if logged in will run script-->
{{#if loggedIn}}
<script src="../public/scripts/comment.js"></script>
{{/if}}



